package app

// mainPageHTML returns a curated article for the EndlessWiki main page.
func mainPageHTML() string {
	return `
<h1>EndlessWiki</h1>
<div class="endlesswiki-body">
  <p><strong>EndlessWiki</strong> is a continuously generated encyclopedia built in <a href="/wiki/go_programming_language">Go</a> with content persisted in <a href="/wiki/mysql">MySQL</a>. It keeps the familiar structure of <a href="/wiki/wikipedia">Wikipedia</a> while filling knowledge gaps on demand with large language models.</p>

  <h2>How it works</h2>
  <p>Each request follows a deterministic pipeline:</p>
  <ol>
    <li>Normalise the slug and check the archive for an existing article.</li>
    <li>If the page is missing, call Groq's <code>moonshotai/kimi-k2-instruct-0905</code> model to draft clean HTML.</li>
    <li>Persist the result so future explorers receive the cached version instantly.</li>
  </ol>

  <h2>Why links matter</h2>
  <p>New pages can only be discovered by following existing links. Direct navigation to unknown slugs is blocked, which keeps the crawl aligned with the graph generated by the model.</p>

  <h2>Continue exploring</h2>
  <ul>
    <li>Review the architecture behind <a href="/wiki/groq">Groq</a> inference.</li>
    <li>Dive into topics on <a href="/wiki/information_retrieval">information retrieval</a> and <a href="/wiki/artificial_intelligence">artificial intelligence</a>.</li>
    <li>Trace the history of collaborative documentation on <a href="/wiki/open_source">open source</a> platforms.</li>
  </ul>
</div>`
}
